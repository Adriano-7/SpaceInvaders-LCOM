
#include "model/game/player.h"
/*
Player* createPlayer(enum GameObject gameObject) {
  Player* player = malloc(sizeof(Player));
  if (player == NULL) {
    printf("Error: malloc failed in createPlayer\n");
    exit(EXIT_FAILURE);
  }

  player->canShoot = true;
  player->lives = 3;
  player->score = 0;
  player->gameObject = gameObject;

  return player;
}*/

